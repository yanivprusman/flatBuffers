{
  "name": "flatbuffers-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild dist/my-game/sample.js --bundle --minify --global-name=MyGame --outfile=dist/my-game.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "typescript": "^5.0.0"
  }
}