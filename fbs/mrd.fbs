
namespace MRD;
enum types:byte {server=0,client=1}
enum actions:byte {getPage=0}
table message {
    type:types;
    action:actions;
    data:string;
}
root_type MRD;
file_identifier "MRDE";
file_extension "mrd";